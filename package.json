{
  "name": "supercompact",
  "version": "1.0.0",
  "description": "Rewrite a Claude Code session down to what was actually said, and keep it resumable",
  "keywords": ["claude", "claude-code", "context", "transcript", "session"],
  "license": "MIT",
  "author": "Adam Albastov",
  "repository": { "type": "git", "url": "git+https://github.com/SignedAdam/supercompact.git" },
  "homepage": "https://github.com/SignedAdam/supercompact",
  "type": "module",
  "bin": { "supercompact": "dist/cli.js" },
  "files": ["dist"],
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "check": "npm run build && ./scripts/check.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
